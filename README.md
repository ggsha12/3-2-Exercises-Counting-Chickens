# 3-2-Exercises-Counting-Chickens
In Chickens01 I learned that if I wanted to change the final result of a bunch of operations involving variables for a different scenario I would have to redefine all the variables that went into the final result

In Chickens02 I learned that Java automatically truncates all decimals if the equation only involves ints, because Java doesn't want to allocate memory it doesn't think is necessary
